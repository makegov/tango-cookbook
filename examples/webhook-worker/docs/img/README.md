# Screenshots

Used by `../../README.md` → "What it looks like". Cropped to the Cloudflare dashboard content (no browser chrome).

| File | View |
| --- | --- |
| `01-deploy-setup.png` | Deploy-to-Cloudflare **"Set up your application"** wizard, with the project name + KV binding pre-filled from `wrangler.jsonc`. The clearest proof the button reads the config. |
| `02-overview.png` | Worker **Overview**: live `workers.dev` URL, the Domains→Worker→Bindings graph, and metrics after the test deliveries. |
| `03-bindings.png` | **Bindings**: the `SEEN_DELIVERIES` KV namespace graph + table. |
| `04-secrets.png` | **Settings → Variables and secrets**, both secrets present with values encrypted. |
| `05-slack.png` | The two opportunity matches the Worker posted to Slack. |
